6. 배열

> Array.length

> 배열 요소에 접근하기

>유사 배열 객체
- Array.from을 사용하면 객체를 배열로 변환 가능
	- Array.from(객체);
- JS의 배열은 객체이다.
- NodeList도 유사 배열 객체이다.

>불변성
- 배열 복사
- 새로운 배열을 봔환하는 메소드 활용
- 원본 배열의 지장을 주면 안된다.

- 배열 복사할 때 
	- filter
	- map
	- slice 등 
- const originArray = [1,2,3];
- const newArray = originArray;
	- 이렇게 하면 newArray는 원본 배열과 일치함
	- 원본 배열의 변화가 생기면 newArray도 바뀜 반대 case도 마찬가지

> for문 배열 -> 고차함수로 리팩토링
- map 메소드를 활용해서 고차 가능하다.

> 배열 메소드 체이닝 활용

>map vs forEach차이
- forEach는 반환 값이 undefined이다.
	- 그냥 배열 내 요소를 반복해서 특정 요소를 돌림
- map은 배열을 반환 해 준다.
	- 원본 배열은 그대로 두고 새로운 배열을 생성
- 새로운 배열 반환 값이 필요 없을 때는 forEach를 사용하는게 좋음

> Continue / break
- 
