>Shorthand Properties

>Computed Property Name

>Loopup Table

>Object.freeze
- 객체를 동결 시킨다.
	- 객체가 변화 없음
- Object.isFrozen으로 동결 됐는지 확인 가능
- 얕은 복사 / 깊은 복사
	- Object.freeze는 깊은 복사는 관여 할 수 없음
	- 객체 내 객체값이 또 있을 때 그 내부 객체는 변화를 할 수 있음

> 프로토타입 조작 지양하기

> HasOwnProperty
- for in 문을 통해서 주로 많이 사용 함
- 객체 프로퍼티를 순회할 때 주로 사용 함

>직접 접근 지양하기
- 객체를 접근하는 함수를 따로 만들어서 적용하는게 좋음
- 예측 가능한 코드를 만들기 위해서 객체가 변화하는 감지를 위해

>Optional Chaining
- ?.연산자
	- .연산자는 객체를 체인으로 탐색 가능하도록 도움
	- 객체에 접근할 때 ?를 붙여서 ?.으로 객체 내 속성에 접근
- 서버에서 받아오는 값을 활용할 때 주로 많이 사용 함
- 
